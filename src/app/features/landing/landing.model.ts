export interface StatItem {
  title: string;
  subtitle: string;
}

export interface FeatureItem {
  title: string;
  description: string;
  borderColor: string;
  iconBg: string;
  iconColor: string;
  icon: string;
}

export interface ReviewItem {
  rating: string;
  name: string;
  review: string;
}
